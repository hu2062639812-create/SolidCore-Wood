# SolidCore Wood

Static Cloudflare Pages site. Deploy this `solidcore-wood` folder as the Pages build output directory (no build command needed).

The enquiry form is a presentation-ready front-end form. Before launch, replace the submit handler in `index.html` with a Cloudflare Pages Function or your CRM/form-service endpoint, and replace the sample sales email address.
